## 容器
server:
    port: 8080 #启动端口

spring:
  application:
    name: RunningGhost
  data:
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: 192.168.43.58:9300
      repositories:
        enabled: true

logging:
       level:
             org.springframework.data: DEBUG
